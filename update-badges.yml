name: Update badges

on:
  schedule:
    # Runs at 0am UTC every day
    - cron: "0 0 * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: AshOConnor/badge-readme@main
        with:
        CREDLY_USER: <ashley-o-connor.97db394f> # optional, but default will use the same from github
